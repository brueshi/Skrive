/* The stb_truetype implementation translation unit. Kept as a C file so the
   Zig side @cImports declarations only; compiling the implementation inside
   a @cImport would duplicate symbols if a second file ever imported it. */
#define STB_TRUETYPE_IMPLEMENTATION
#include "stb_truetype.h"
